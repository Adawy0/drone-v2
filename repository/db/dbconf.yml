test:
    driver: postgres
    open: user=postgres dbname=drone password=postgres sslmode=disable

development:
    driver: postgres
    open: host=localhost user=postgres password=postgres dbname=drone port=5432 sslmode=disable
    
production:
    driver: postgres
    open: user=user dbname=dbname sslmode=verify-full

environment_variable_config:
    driver: postgres
    open: host=localhost user=postgres password=postgres dbname=drone port=5432 sslmode=disable
